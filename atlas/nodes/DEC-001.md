Prod SaaS deploy dari branch 'vercel' (bukan main). api/index.php jadi Vercel entry: bypass route manual via $router (Route Facade gagal di PHP 8.5), webhook direct handling, auto-migrate per deploy.
