Split bill modal showed 0 items. Alpine x-data initialized at page render (empty cart), Livewire morph doesn't re-init Alpine. Fix: hidden script tag + dispatch event + window.addEventListener in init.
